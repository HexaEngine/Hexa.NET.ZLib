name: Publish NuGet Packages

on: 
  push:
    tags:
      - 1.**
  workflow_dispatch:
  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: |
          dotnet restore Hexa.NET.ZLib/Hexa.NET.ZLib.csproj

      - name: Build the project
        run: |
          dotnet build Hexa.NET.ZLib/Hexa.NET.ZLib.csproj --configuration Release

      - name: Pack the project
        run: |
          dotnet pack Hexa.NET.ZLib/Hexa.NET.ZLib.csproj --configuration Release --output ./nupkgs

      - name: Exclude unwanted packages
        run: |
          # Exclude packages that don't start with "Hexa.NET."
          for package in ./nupkgs/*.nupkg; do
              package_name=$(basename "$package")
              if [[ ! $package_name == Hexa.NET* ]]; then
                  echo "Removing $package_name as it does not start with Hexa.NET."
                  rm -f "$package"
              fi
          done
          
      - name: List packages
        run: ls ./nupkgs/

      - name: Publish the package
        run: dotnet nuget push ./nupkgs/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
